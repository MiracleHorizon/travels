# Инструкция по запуску и тестированию

## Запуск приложения

### 1. Запуск Backend

```bash
cd backend
bun install  # если еще не установлены зависимости
bun server.ts
```

Сервер должен запуститься на `http://localhost:3000` и вывести:
```
🚀 Server running at http://localhost:3000
```

### 2. Запуск Frontend

В новом терминале:

```bash
cd frontend
bun install  # если еще не установлены зависимости
bun run dev
```

Приложение откроется на `http://localhost:5173`

## Тестирование функциональности

### Создание путешествия

1. Откройте приложение в браузере
2. Нажмите кнопку "Новое путешествие"
3. Заполните форму:
   - Направление: например, "Токио, Япония"
   - Выберите даты через календарь
4. Нажмите "Создать"

### Вкладки и фильтрация

После создания путешествий вы увидите 3 вкладки:

1. **Будущие** - путешествия с датой окончания >= текущей даты
2. **Прошедшие** - путешествия с датой окончания < текущей даты
3. **Архив** - архивированные путешествия (любого статуса)

### Операции с путешествиями

Для каждого путешествия доступны действия:

- 🗄️ **Архивировать/Разархивировать** - переместить в архив или вернуть обратно
- ✏️ **Редактировать** - изменить направление или даты
- 🗑️ **Удалить** - удалить путешествие

### Детальная страница путешествия

При клике на карточку путешествия открывается детальная страница с тремя вкладками:

#### 1. Маршрут по дням (Itinerary)

Позволяет создать детальное расписание путешествия по дням:

- **Автоматическое разделение по дням** - для каждого дня путешествия создается отдельная секция
- **Добавление мест в расписание** - выберите место из списка и укажите время посещения
- **Временные слоты** - для каждого места указывается время начала и окончания
- **Время в пути** - можно указать оценку времени на перемещение до следующей точки
- **Заметки** - дополнительная информация о посещении места
- **Визуальная временная шкала** - наглядное отображение последовательности посещений
- **Редактирование и удаление** - изменение времени или удаление места из маршрута

Как использовать:
1. Перейдите на страницу путешествия
2. Откройте вкладку "Маршрут по дням"
3. Выберите день и нажмите "Добавить место"
4. Выберите место из списка
5. Укажите время начала и окончания посещения
6. Опционально: добавьте время в пути до следующего места и заметки
7. Нажмите "Добавить"

#### 2. Места для посещения

Список всех мест, которые вы хотите посетить в этом путешествии:

- **Добавление мест** - укажите название, комментарий, ссылки и фотографии
- **Карточки мест** - красивое отображение с фотографиями
- **Удаление мест** - удаление ненужных мест

#### 3. Детали поездки

Подробная информация о путешествии:

- **Timeline дат** - визуальное отображение начала и окончания
- **Метаданные** - информация о создании и обновлении

### Автоматическое определение статуса

Статус путешествия определяется автоматически:
- Если дата окончания >= сегодня → статус "upcoming" (будущее)
- Если дата окончания < сегодня → статус "past" (прошедшее)

При изменении дат путешествия статус обновляется автоматически.

## Тестирование API напрямую

### Создать путешествие

```bash
curl -X POST http://localhost:3000/api/travels \
  -H "Content-Type: application/json" \
  -d '{
    "destination": "Париж, Франция",
    "startDate": "2026-06-01",
    "endDate": "2026-06-10"
  }'
```

### Получить все путешествия

```bash
curl http://localhost:3000/api/travels
```

### Получить путешествие по ID

```bash
curl http://localhost:3000/api/travels/{id}
```

### Обновить путешествие

```bash
curl -X PUT http://localhost:3000/api/travels/{id} \
  -H "Content-Type: application/json" \
  -d '{
    "destination": "Лондон, Великобритания"
  }'
```

### Архивировать путешествие

```bash
curl -X PUT http://localhost:3000/api/travels/{id} \
  -H "Content-Type: application/json" \
  -d '{
    "isArchived": true
  }'
```

### Удалить путешествие

```bash
curl -X DELETE http://localhost:3000/api/travels/{id}
```

## Проверка работы вкладок

1. Создайте путешествие с датами в будущем → оно появится во вкладке "Будущие"
2. Создайте путешествие с датами в прошлом → оно появится во вкладке "Прошедшие"
3. Архивируйте любое путешествие → оно переместится во вкладку "Архив"
4. Разархивируйте путешествие → оно вернется в соответствующую вкладку (Будущие/Прошедшие)

## Возможные проблемы

### Backend не запускается

- Убедитесь, что порт 3000 свободен
- Проверьте, установлен ли Bun: `bun --version`

### Frontend не подключается к Backend

- Проверьте, что backend запущен на порту 3000
- Откройте консоль браузера (F12) и проверьте ошибки сети
- Убедитесь, что CORS настроен правильно (уже настроен в коде)

### Путешествия не отображаются

- Откройте консоль браузера и проверьте ошибки
- Убедитесь, что API возвращает данные: `curl http://localhost:3000/api/travels`
